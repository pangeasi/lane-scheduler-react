import type { LaneConfig } from "../types";

export const DEFAULT_CONFIG: Required<LaneConfig> = {
  height: 80,
  slotWidth: 60,
  slotColor: "#f3f4f6",
  slotBorderColor: "#e5e7eb",
  snapThreshold: 0.5,
};
